---
description: Link a task to an external workspace — dk project, GitHub repo, Confluence page, etc.
argument-hint: "<task-name> <type>:<id>"
---
Read ~/.pi/agent/skills/tasks/SKILL.md and follow the /tasks-link workflow.
Link task "$1" to workspace $2 by adding to the ## Workspaces section in the task body.
To remove: use /tasks-link "$1" --remove "$2"
