# Users

User-specific configurations and personal settings.

Each user directory contains:
- Personal dotfiles and configurations
- User-specific home-manager settings
- Private configurations (git user info, SSH keys, etc.)

These configs are imported by home-manager in host configurations.
